{% extends 'base.html.twig' %}

{% block title %}ObjectHS{% endblock %}

{% block body %}
    <h1>ObjectHS</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ object_h.id }}</td>
            </tr>
            <tr>
                <th>CreationDate</th>
                <td>{{ object_h.creationDate ? object_h.creationDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>ModificationDate</th>
                <td>{{ object_h.ModificationDate ? object_h.ModificationDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ object_h.status }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ object_h.name }}</td>
            </tr>
            <tr>
                <th>State</th>
                <td>{{ object_h.state }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ object_h.age }}</td>
            </tr>
            <tr>
                <th>Details</th>
                <td>{{ object_h.Details }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_object_h_s_index') }}">back to list</a>

    <a href="{{ path('app_object_hs_edit', {'id': object_h.id}) }}">edit</a>

    {{ include('object_hs/_delete_form.html.twig') }}
{% endblock %}
