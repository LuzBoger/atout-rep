{% extends 'base.html.twig' %}

{% block title %}HomeRepair index{% endblock %}

{% block body %}
    <div class="container mx-auto mt-10">
        <h1 class="text-2xl font-bold text-primary-700 mb-6">Historique des demandes de réparation maison</h1>

        {% include 'components/tableHomeRepair.html.twig' %}

        <div class="mt-4 flex gap-4">
            <a href="{{ path('app_roofing_new', { 'type': 'roofing' }) }}"
               class="inline-block px-6 py-2 bg-primary-700 text-white rounded-lg hover:bg-primary-800">
                Ajouter une toiture
            </a>
            <a href="{{ path('app_painting_new', { 'type': 'painting' }) }}"
               class="inline-block px-6 py-2 bg-primary-700 text-white rounded-lg hover:bg-primary-800">
                Ajouter une peinture
            </a>
        </div>
    </div>
{% endblock %}
